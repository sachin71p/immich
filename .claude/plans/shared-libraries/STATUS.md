# Status

Legend: ⬜ todo · 🟨 in progress · ✅ done · ⛔ blocked · ➖ skipped

| Phase | Title | Depends on | Risk | Status | Commit | Note |
|---|---|---|---|---|---|---|
| S0 | Fork baseline, FORK.md, branch | — | low | ✅ | 3e0c08539 | base e55ac299a (v3.1.0+379 per git describe; package 3.2.0 is the dev version, not a tag); server+web check/lint/test PASS after plugin-sdk bootstrap |
| S1 | Schema & migrations | S0 | med | ✅ | 06ec13fac | targeted medium schema tests PASS; full unit suite blocked by sandbox socket policy |
| S2 | Access control, favorites & album permissions | S1 (parallel-safe with S3) | **high** | ✅ | f34f92116 | 16-Sep verified: upstream `album.e2e-spec.ts` 40/40 PASS on fork code (default stack) — the 5 known failures (viewer add/remove, owner denied, role-change) are resolved by the remediation pile, confirmed fork-side regressions, now fixed |
| S3 | Storage keys & relocation engine | S1 (parallel-safe with S2) | **high** | ✅ | 83f80f33f | check/lint and focused suites PASS; full unit suite blocked by sandbox socket policy; remediation 16-Sep changed the engine (G1/G2: throw-on-failure, containment, R10-02/R17-01) — host medium/e2e re-run needed |
| S4 | Spaces/library-member API, move API, upload target, lifecycle | S2, S3 | **high** | ✅ | 19cce54f5 | typecheck/lint and focused suites PASS; full unit suite blocked by sandbox socket policy; Dart SDK regen needs Java; remediation 16-Sep added container-deletion tombstones (`d93fc777d`) + e2e harness fixes (`aa7923248`) — host e2e re-run needed |
| S5 | Visibility scope across queries | S4 | **high** | ✅ | 98f0179d9 | shared-link TimelineRead gate restored; HOST medium #3 15-Sep timeline.service 12/12 PASS (incl. the shared-link 400 test); remediation 16-Sep fixed stale R8-04 unit booleans (`670202347`) — unit green, host re-run pending |
| T0 | Test harness: fixtures, world, disk oracle, runner, coverage | S0 (parallel-safe with S6/S7) | med | ✅ | 83f7fc363 | 16-Sep verified: SY 3/3 + timeline 11/11 + integrity 18/18 (default stack) + media/transcoding units 253/253 green; workflow suite 22/22 green after building plugin.wasm from pinned upstream binaries (no mise, gitignored dist); no audio-video e2e suite exists in repo (media/transcoding units are the coverage); fork-stack integrity not run by design (TESTING.md §6: upstream specs hardcode /data, upstream tier runs default stack); CLI dist present; watch-mode `727b68e38` present; boot-race ping wait working; coverage tier PASS |
| T1 | Regression backfill for S1–S5 (host run with Docker) | T0 | **high** | ✅ | | 16-Sep verified: all 53 T1 IDs audited — E covered by fork specs, M covered (MV-02/03, INV-01), 11 dual E,U closed (untagged unit refs tagged; R2-02/R2-03 missing paths added, 210 unit green); R13-04 present + ML-gated skip observed; W-01–W-09 present and EXECUTED green 13/13 (web fixes: Add-button scoping, admin queue token, accordion expand, dropdown scoping, save-wait; `ensureLoaded` in-flight race fixed in shared-spaces store); R4-01 fixed `f6bd80b62` + `4a3e65308`, moves 6/6 + full suites green; R17-03 `auditDiskStable` (`f6177097a`); AP-03/05/06 retagged (`9d1454a5f`); R16-02 matrix corrected to 400. Tally: medium PASS, e2e-api on+off 75/1-skip, web 13/13, coverage PASS. Pre-existing `swift test` break: SearchTests.swift:186 arg-order, A7 file, untouched |
| S6 | Sync stream for spaces & shared libraries | S5 | **high** | ✅ | 41822db64 | ambiguous upserts + create/update split (re-keyed on createdAt<=checkpoint-time) fixed; HOST medium #3 15-Sep sync-shared-container-asset 3/3 PASS (SY-01..05); fork world e2e R17 still red (T0 oracle agent dispatched); remediation 16-Sep added deletion tombstones + [LC-01]/[C3] medium tests (`d93fc777d`) — host medium re-run needed |
| S7 | Full metadata endpoint & extended search filters | S5 | med | ✅ | 231211358 | check/lint and 241 configured focused tests pass; bare plan Vitest command lacks alias config; full/medium suites constrained by sandbox/container |
| S8a | Web: spaces management, settings, admin library members | S6, S7 | med | ✅ | 0d317e4ee | check:typescript/lint clean (fixed 5 lint errors + 2 tailwind warnings); check:svelte and vitest blocked by host toolchain (pre-existing TS 6.0.3 crash, localStorage-in-vitest gap), not phase defects |
| S8b | Web: timeline switcher, move action, favorites/album UI, upload target | S8a | med | ✅ | c688cca6e | 16-Sep verified: fork web e2e 4/4 green (W-01×2, W-02, W-09); full web project 37 passed/1 skipped with only those 3 fork tests failing pre-fix; web unit 359 passed/0 failed (42 files with pre-existing localStorage setup errors, unchanged) |
| S8c | Web: full metadata panel, search filters | S8a (parallel-safe with S8b) | low | ✅ | 1c552f852 | 16-Sep verified: same web evidence as S8b (fork e2e 4/4 green incl. W-02 settings toggles; unit 359/0 with pre-existing localStorage file-errors) |
| S9 | (optional) Space-scoped People | S6 | high | ✅ | 1e5d39f46 | code checks PASS; HOST unit PASS 15-Sep (person-space 10/10); HOST medium #3 15-Sep person.service 21/21 PASS; HOST e2e-api person.e2e-spec 16/16 PASS; remediation 16-Sep gated person/face owner access on PERM-11 (`7b77c2860`, S9 unit still 10/10 in sandbox) — host medium/e2e re-run needed |
| S10 | Hardening: upgrade gate, FORK.md, upstream-merge rehearsal | S8b, S8c, T1 | med | ✅ | 16-Sep verified: SQL regen current (no changes, order 98); INV-02 openapi-diff PASS (+12 paths/+23 schemas); UP-01 PASS (data intact, reserved-label guard) after `upgrade.sh` `migrations`→`kysely_migrations` fix (worktree-uncommitted); OpenAPI regen unblocked — S7 number fields got `double` format + spec guard, spec regenerated additively. Parked (deliberate, A9.7-style): `updateMyTimeline`→`updateMySpaceTimeline` rename spans ~11 files + SDK/spec regen incl. native-apple in concurrent user edits — stays deferred per AUDIT-FINDINGS Task 4 |
| A0 | Apple: workspace, shared core package skeleton, codegen | S0 | low | ✅ | 9d686e0b8 | host now has a full Xcode toolchain; `swift build && swift test` verified clean (1 fixed `Operations.Login`→`Operations.login` naming bug, applied in A1) |
| A1 | Apple core: auth, sync engine, local DB | A0, S6 | high | ✅ | 2756651c4 | `swift build && swift test` clean on independently reverified from-scratch build: 29/29 pass; AP-02 vs rules-cases.json deferred to T0 |
| A2 | Apple core: image pipeline + cache tiers | A1 | med | ✅ | 6618a19c5 | swift build + swift test 55/55 PASS (29 A0/A1 intact); verify.sh sandbox-exec + xcodegen NOT RUN (toolchain gaps) |
| A3 | iOS app: grid, viewer, libraries, albums, favorites, move | A2 | med | ✅ | | implementer DONE 15-Sep; verifier PARTIAL 15-Sep (handoff/A3-verify.md): verify.sh ios NOT RUN, xcodegen missing in sandbox; host `verify.sh ios` run 15-Sep found 6 build breaks fixed: (1) LocalStore+Search.swift:93 Sendable capture of mutable `var stringArgs`/`mixedArgs` in a `dbQueue.read` closure — hoisted to a `let sqlArgs` before the closure; (2) Search/FilterDSL.swift:71 `ISO8601DateFormatter` static-let concurrency-safety — `nonisolated(unsafe)`, matching the existing SyncEngine/WireTypes.swift pattern; (3) BackupScanner.swift/BackupScheduler.swift used `import PhotoKit`/`canImport(PhotoKit)`, but the real module is `Photos` (PhotoKit is just the tech's marketing name) — always-false `canImport` meant `PhotoKitBackupScanner` never compiled, breaking BackupSettings.swift; fixed imports, plus added `import PhotosUI` for `presentLimitedLibraryPicker(from:)` which lives in a PhotosUI category, not core Photos; (4) BackupScheduler.swift:24 `BGProcessingTask` (non-Sendable ObjC class) captured across an actor boundary — `@preconcurrency import BackgroundTasks`; (5) SearchView.swift missing two `await`s on an async `setRows` call; (6) SearchView.swift/MacSearchView.swift `ForEach(exif.matching(...), id: \.self)` over a non-Hashable tuple — switched to `.enumerated()`/`id: \.offset`; (7) EditView.swift lock-guarded `static var cache` needed `nonisolated(unsafe)`, and a `.secondary : .yellow` ternary needed both arms wrapped in `AnyShapeStyle` to unify the inferred `ShapeStyle` type. Build+bootstrap now PASS; `A3SmokeUITests.testGridViewerAndMoveSheet` PASS, confirmed stable across 2 consecutive full `verify.sh ios` runs (exit code 0 both times). |
| A4 | macOS app: sidebar, grid, viewer, libraries, albums, move, import | A2 (parallel-safe with A3) | med | ✅ | | host `verify.sh mac` 15-Sep: build FAILED → fixed 6 Swift 6 concurrency/init-order bugs (MacSidebar/MacAppState/MacGridView/MacMainWindow) + WindowGroup(for:) never auto-opening a window (no `openWindow` call at launch, fixed via app-delegate) + `.task` on ProgressView self-cancelling on state swap; build+bootstrap now PASS. MacSmokeTests 3/3 PASS (stable across 2 reruns). The move/add-to-album sheets used `.sheet(isPresented: Bool)` + a sibling `[String]` `@State` var set in the same action — real SwiftUI race where the sheet's content closure could capture the sibling state from the render *before* the update, presenting with an empty/stale selection (reproduced via targeted NSLog: `moveIds` held 6 ids, the sheet closure read `[]` 4ms later). Fixed both sheets to the `.sheet(item:)` pattern (`AssetIdsSheetItem`, matching the already-correct `managingSpace` sheet) so the trigger and its data commit atomically. Also cleared a stale `~/.Trash` app copy and a stale AppKit window-frame-autosave pref that were blocking the test runner — host-local, not repo state. Re-run 15-Sep after new A5/A7 source landed (MacImport.swift camera/SD import via ImageCaptureCore, MacGridView `.search` destination, MacEditView) surfaced a fresh set of build breaks, fixed: MacGridView.swift:59 `switch destination.query` non-exhaustive after `DestinationQuery.search` was added (routes to `MacSearchView` separately, not the grid) — added `.search` alongside `.map, .people, .memories` returning `[]`; MacEditView.swift:303 called `macThumbCG(style: style)` against a function declared with an unlabeled parameter — dropped the label; MacImport.swift's `MacCameraBrowser` had a long tail of ImageCaptureCore API-surface mismatches against the actual macOS 27 SDK headers (verified against `ICCameraItem.h`/`ICCameraFile.h`/`ICDevice.h`/`ICDeviceBrowser.h` and confirmed via isolated `swiftc -typecheck` probes): `ICCameraItem` has no `fileSize`/`thumbnailData` (those are `ICCameraFile`-only via `fileSize` and the async `requestThumbnailData(options:completion:)`, not a stored property) — cast to `ICCameraFile` and made `CameraThumbnail` load its image asynchronously via `.task`; the old selector-based `requestDownloadFile(_:options:downloadDelegate:didDownloadSelector:contextInfo:)` no longer matches this SDK's Swift signature — replaced with the block-based `requestDownload(options:completion:)` and deleted the now-unused `DownloadDelegate` NSObject trampoline; `ICDownloadsDirectoryURL`/`ICSaveAsFilename` global constants are now namespaced as `ICDownloadOption.downloadsDirectoryURL`/`.saveAsFilename`; `ICDeviceBrowserDelegate`/`ICDeviceDelegate` conformances crossed into main-actor-isolated code (same pattern as the earlier MacGridView Coordinator fix) — annotated both conformances `@MainActor` in the extension's inheritance clause; `MacCameraBrowser` didn't conform to `ICDeviceDelegate` (missing the two `@required` methods `device(_:didCloseSessionWithError:)` and `didRemove(_:)` — not `didRemoveDevice`, which is a renamed/removed selector) — implemented both; `camera.mediaFiles` is `[ICCameraItem]?` but was assigned into a non-optional `@Published var items` — added `?? []`; and `ICDevice.requestOpenSession()` resolves to an async-throws overlay on this SDK, not the plain sync ObjC method — added `try await` at both call sites (one inside an already-async func, one wrapped in `Task { try? await ... }` since `deviceBrowser(_:didAdd:)` is a sync delegate callback). Build+bootstrap PASS; MacSmokeTests 3/3 PASS, confirmed stable across 2 consecutive full `verify.sh mac` runs (exit code 0 both times). |
| A5 | Backup/upload (iOS extension + macOS watcher/import) | A3, A4 | high | ✅ | e8864bc11 | host `verify.sh ios`+`mac` green combined with A7/A8; 7 user build-break fixes folded in |
| A6 | Storage optimization (iOS free-up-space R14, macOS cache budget) | A5 | high | ✅ | d19498632 | implementer DONE 15-Sep; host `verify.sh ios`+`mac` green combined with A9 (user fixes folded in); AP-03 coverage tag backfilled 16-Sep (`9d1454a5f`), coverage tier PASS |
| A7 | Metadata panel, search & filters (both) | A3, A4, S7 | low | ✅ | a647d21b9 | host `verify.sh ios`+`mac` green combined with A5/A8 |
| A8 | Editor engine (Core Image) + UIs | A3, A4 | high | ✅ | 11200a436 | host `verify.sh ios`+`mac` green combined with A5/A7; shared wiring rode along; AP-05 tagged 16-Sep on synthetic determinism tests (`9d1454a5f`) — `@personal portrait.heic` half pending personal fixtures |
| A9 | Photos-like extras: Live Photos, video, Live Text, memories, map, extensions | A8 | med | ✅ | 0738bbae1 | implementer DONE 15-Sep (A9.1–A9.6; A9.7 experimental parked); host `verify.sh ios`+`mac` green combined with A6; AP-06 tagged 16-Sep on synthetic live-pair test (`9d1454a5f`) — `@personal live.heic/.mov` half pending personal fixtures |

Rename 15-Sep: PhotosFork→Heirloom everywhere (display name, targets/schemes, bundle IDs `com.immich.heirloom.*`, sources, verify.sh) — 94f648aad. Host re-verified 16-Sep post-rename: user-run `verify.sh ios` + `verify.sh mac` from repo root, both green (schemes Heirloom-*).
