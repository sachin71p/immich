# A3 handoff — iOS app: library, collections, viewer, move

## Summary
Implemented all 9 A3 tasks as a thin iOS presentation layer over PhotosCore/A2.
Grid, switcher, collections, viewer, selection/move, spaces, albums, settings,
and permission gating are present; focused Apple-tier backing tests pass.

## Files changed
New: `Apps/iOS/Sources/{AppSession,FixtureSeed,LibraryGrid,MoveSheet,Viewer,Collections,Spaces,Albums,Settings}.swift`,
`Apps/iOS/UITests/A3SmokeUITests.swift`, `PhotosCore/Sources/LocalStore/LocalStore+Browse.swift`,
`PhotosCore/Tests/A3SmokeTests.swift`.
Modified: `Apps/iOS/Sources/HeirloomIOSApp.swift`, `Apps/Shared/ConnectView.swift`,
`PhotosCore/.../CoreModel/Timeline.swift`, `LocalStore+{Mutations,Timeline}.swift`,
`SyncEngine/{Asset,Album,Space}Mutations.swift`, `ImmichAPI/openapi-generator-config.yaml`,
`native-apple/project.yml`.

## Deviations
- Shared `ConnectView` now persists server URL for session reload.
- Made timeline row/scope helpers internal for shared browse queries.
- Wired album share/remove-user endpoints; space timeline toggle/transfer/refresh added.
- iOS multi-share/viewer copy use authorized original downloads; video copy uses movie UTI.
- Library timeline toggles remain read-only; no wired library-member endpoint.

## Open issues
- `verify.sh ios` NOT RUN: `xcodegen` is missing in sandbox; Docker tiers NOT RUN.
- Coordinate/consolidate duplicate PhotosCore browse helpers with parallel A4 work.
- Live-photo motion assembly remains A9; Places uses online map tiles.
- Core: `swift test --disable-sandbox`: 65/65 pass; shimmed iOS typecheck clean.
