# T1 handoff — regression backfill (continued prior attempt; E specs kept, fixed, completed)

## Files added
- `server/test/medium/specs/fork/asset-relocation.spec.ts`: [MV-02] fail→attempts=1→retry clean, [MV-03] bootstrap queue-all finishes
- `server/test/medium/specs/fork/library-scan-guard.spec.ts`: [R10-02] watcher unlink + scan-offline guards
- `server/src/dtos/search.dto.spec.ts`: [R8-04] filter mutual-exclusivity (only genuine U gap; all other U rows covered by S2–S5 specs)
- `e2e/src/specs/web/fork/{spaces,settings,library-members}.e2e-spec.ts`: [W-01], [W-02], [W-09]
- Prior attempt's 6 E specs kept; fixed 23 tsc + 18 lint errors (SDK-enum move targets, preferences arg shape). Tagged [INV-01] onto existing S1 schema test (no duplicate).

## Self-check
- `server tsc --noEmit`: PASS. `e2e tsc`: PASS (was 23 errors).
- eslint touched files (server + e2e fork): PASS.
- `vitest search.dto.spec + library-upload-path.spec`: 10/10 PASS.
- `run.sh coverage`: all T1 + S1–S5 + R13-04/W-01/W-02/W-09 tagged; only MISSING is SY-06 (S6-owned gate, out of scope).

## BUG: (infra, test-observed)
- R6-02 sync part: SDK lacks S6 sync types (`SharedSpaceAssetsV1`); spec casts raw string. Expected: regen. Suspected: `packages/sdk/src/fetch-client.ts` (+ open-api spec) via sync-open-api.
- W-09 persistence asserted via toast only: `LibraryResponseDto` exposes no `uploadPath` (already in S8a handoff).

## NOT RUN (no Docker in sandbox)
- medium (`server/test/medium/specs/fork`), e2e-api (fork E), e2e-web (fork W). Authored honestly; host run: `run.sh unit medium e2e-api coverage --template both`.
- Note: files staged under this session by parallel host activity, not by me; `git add` never run.
