# S2 handoff — Access control, favorites, album permissions

## Summary
Implemented container-aware asset and file access, shared-space/library access helpers,
shared favorites, permissive album asset membership, album-role safeguards, and Locked
visibility checks for shared assets.

## Files changed
- New: `server/src/utils/access.spec.ts`.
- Upstream-modified: `server/src/{enum.ts,utils/access.ts,repositories/access.repository.ts}`.
- Upstream-modified: `server/src/services/{asset.service.ts,album.service.ts}`.
- Upstream-modified tests/mocks: `server/src/services/{asset.service.spec.ts,album.service.spec.ts}`, `server/test/repositories/access.repository.mock.ts`.

## FORK.md patch-list lines
`server/src/{enum.ts,utils/access.ts,repositories/access.repository.ts} | grants container-aware asset and shared-space access | S2 | S2`
`server/src/services/{asset.service.ts,album.service.ts} | protects shared favorites, roles, and Locked visibility | S2 | S2`

## Deviations
- `asset.util.ts` remains generic; the album service now passes `AlbumAssetDelete` to its existing generic removal helper so any album member can remove any album asset.

## Open issues
- `cd server && pnpm run check` and `pnpm run lint` pass.
- Focused unit tests pass: access, asset service, and album service (128 tests).
- `mise //server:sql` and `mise //server:sync-open-api` could not run because `mise` is not installed in this environment; regenerate SQL/OpenAPI during verification.
- Verification round 1 confirms `pnpm run check` passes; lint failures are confined to S3-owned relocation/library files, so S2 requires no code changes.
