# S5 handoff — Visibility scope across queries

## Summary
Implemented `ContainerScope`, SQL predicate, and resolver honoring preferences, memberships, explicit filters,
partner timeline behavior, management, and locked scopes. Applied it to timeline, legacy/V3 search, suggestions,
city/explore, map, statistics, calendar heatmap, memories generation, and trash. Added shared-space favorite
projection for timeline/API/sync album assets.

## Files changed
- New: `server/src/utils/container-scope.{ts,spec.ts}`.
- Upstream-modified: `server/src/{dtos/{asset-response,asset,calendar-heatmap,map,search,time-bucket}.dto.ts,repositories/{asset,map,search,sync,trash}.repository.ts,services/{asset,map,memory,search,timeline,trash,user}.service.ts,services/shared/user-methods.ts,utils/database.ts,services/index.ts,test/utils.ts}`.

## FORK.md patch-list lines
`server/src/utils/container-scope.ts | visibility resolver and SQL scope predicate | visibility §10 | S5`
`server/src/repositories/{asset,search,map,trash}.repository.ts | container-scoped listings and trash mutations | visibility §10 | S5`
`server/src/services/{timeline,search,map,asset,trash,user,memory}.service.ts | resolve/apply request visibility scope | visibility §9-10 | S5`

## Deviations / open issues
- `mise` is unavailable. SQL regeneration via a manually built `sync-sql` failed because query recording could not
  connect; it deleted generated files, which were immediately restored. SQL/OpenAPI/TS/Dart SDK regeneration remains
  required in the normal mise-capable environment.
- No Docker medium test was run. Focused unit coverage validates resolver purposes, empty hidden membership, explicit
  access filter, and stale-filter 403; it does not supply the requested seeded end-to-end matrix.

## Self-check
- `cd server && pnpm run check` — pass.
- `pnpm run lint` — pass after targeted project-formatter run on S5-owned files (verification round 2's 211 Prettier errors resolved).
- `pnpm exec vitest --config test/vitest.config.mjs src/utils/container-scope.spec.ts src/services/{timeline,search,map,asset,trash,user,memory}.service.spec.ts` — 181/181 pass.
