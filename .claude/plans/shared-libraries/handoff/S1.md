# S1 handoff — Schema & migrations

## Summary
Completed the staged S1 schema implementation: shared spaces/members, library members and upload paths,
asset container invariants, relocation/audit tables and triggers, audit pruning, generated migration, and
medium DB coverage for the CHECK plus shared-space asset audit triggers.

## Files changed
- New: `server/src/schema/tables/{shared-space,shared-space-member,library-member,asset-relocation}.table.ts`.
- New: `server/src/schema/tables/{shared-space-audit,shared-space-member-audit,shared-space-asset-audit,library-member-audit,library-asset-audit}.table.ts`.
- New: `server/src/schema/migrations/1789426700279-SharedLibraries.ts`; `server/test/medium/specs/repositories/shared-libraries-schema.spec.ts`.
- Upstream-modified: schema index/enums/functions, asset/library tables, migration ORDER; sync audit cleanup,
  asset typing/factories, and its fixture.

## FORK.md patch-list lines
`server/src/schema/{index,enums,functions}.ts | registers shared-library schema/audit hooks | S1 | S1`
`server/src/schema/tables/{asset,library}.table.ts | adds container fields/invariants | S1 | S1`
`server/src/{repositories/sync.repository,services/sync.service}.ts | prunes new audit tables | S1 | S1`

## Deviations
CODEMAP-FIX: §C does not identify an upstream `(ownerId, timeline date)` asset index; upstream has none.
S1 adds `(spaceId, localDateTime)` for space timeline access.

## Open issues
`pnpm run check` and `pnpm run lint` pass. `pnpm run migrations:generate` reports no changes.
The verifier's unit failure is unrelated to S1: 34 controller suites all fail before assertions when
Supertest cannot bind `0.0.0.0` (`listen EPERM`); S1 changes no HTTP/server/listen code.
With container-runtime access, `pnpm exec vitest --config test/vitest.config.medium.mjs --run
test/medium/specs/repositories/shared-libraries-schema.spec.ts` passes (1 file, 5 tests), including applying
`1789426700279-SharedLibraries` to an empty test database.
