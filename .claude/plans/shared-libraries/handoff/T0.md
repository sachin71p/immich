# T0 handoff — Test harness, fixtures, world, runner

## Summary
Harness complete and sandbox-verified: 31 synthetic fixtures generated +
verified against `manifest.json` (byte-deterministic re-runs proven);
world builder, disk oracle, job/sync/as/record helpers, `[R17-01]`/`[R17-03]`
smoke spec, `run.sh` runner, coverage checker, fork compose override,
CI workflow, FORK.md gate. `e2e tsc`, `server check`, `bash -n` clean.

## Files changed
- New: `server/test/fork-fixtures/generate.ts`, `e2e/fork-assets/{manifest.json,
  generated/* (31+4 xmp),rules-cases.json (50 rows),personal/README.md}`,
  `e2e/src/specs/server/api/fork/{world,disk,jobs,sync,as,record-sync}.ts`,
  `world.e2e-spec.ts`, `e2e/docker-compose.fork.yml`,
  `scripts/fork-test/{run.sh,coverage.ts,index-personal.ts}`,
  `.github/workflows/fork-tests.yml`.
- Upstream-modified: `.gitignore` (fork block), `FORK.md` (gate §, no table rows).
- Submodule `e2e/test-assets` initialized (6742055).

## FORK.md patch-list lines
None (no upstream-file behaviour change; gate section only).

## Deviations
- T0 task 6's unit spec belongs at `server/src/utils/fork-rules-cases.spec.ts`
  (S9-owned tree) → NOT created; `rules-cases.json` done, spec is open issue 1.
- `disk.ts` template-ON uses container-prefix search, not exact template
  rendering (upstream's business; fork invariant is container placement).
- No `tsx` in repo: `run_ts`/headers use stage-under-server + tsc + node.
- Coverage ID regex fix: prefixes need `[0-9]*` (`R1-01` vs `SY-01`).

## Open issues
1. ADD `server/src/utils/fork-rules-cases.spec.ts` (runs `rules-cases.json`
   vs real access/move/scope fns; also unblocks AP-02 Swift parity, A1 note).
2. Coverage FAILS by design until T1: done-phase cases lack `[ID]` tags —
   R16-04/R12/R13/SY-*/W-01/W-02/W-09 (S6/S7/S8a specs exist, untagged).
3. S3 uses `shared/<spaceId>` derived keys vs DECISIONS §7
   `library/shared/<label>` originals — disk oracle follows DECISIONS text.
4. NOT RUN (no Docker/sockets): `run.sh unit` (full), `medium`, `e2e-api`,
   `e2e-web`, `upgrade`; `@personal` skipped (no personal files).
