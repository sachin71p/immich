# S0 handoff — Fork baseline

## Summary
Added `upstream` remote (https://github.com/immich-app/immich.git), fetched tags, created
branch `feat/shared-libraries` from `main`, wrote `FORK.md` at repo root with purpose, base,
merge procedure, cadence, and empty patch-list table. Staged (not committed) `.claude/plans/`
and `FORK.md`.

## Upstream base
- Tag: `v3.1.0`
- Sha: `e55ac299a4ec7cb372e35dbf2c6c05ee9ce77f6c` (current `main` HEAD; note this commit is a
  few commits ahead of the `v3.1.0` tag object itself — `git describe --tags --abbrev=0`
  resolves to `v3.1.0` as the nearest ancestor tag).

## Files changed
- New: `FORK.md` (repo root).
- New (spec, not code): `.claude/plans/shared-libraries/**` (previously untracked; now staged).
- Upstream-modified: none.

## FORK.md patch-list lines
None yet — table header only (`| upstream file | change | reason | phase |`), no rows.

## Deviations
None.

## Open issues
None. `docker`, `pnpm`, and verify commands were intentionally not run (out of scope for this
implementer pass — verifier agent runs `handoff/S0-verify.md` per the brief's Verify section).

## Staged, not committed
`.claude/plans/` and `FORK.md` are `git add`-ed on branch `feat/shared-libraries` but not
committed. The orchestrator commits.
