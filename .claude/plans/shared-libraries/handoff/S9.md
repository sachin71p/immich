# S9 handoff — Space-scoped People

## Summary
- `person.spaceId` (nullable, FK CASCADE): NULL = personal per-owner (upstream); set = one row visible to all space members.
- Space faces cluster only within their space (dedicated `cluster_group` per space via `shared_space.clusterGroupId`); moves across a space boundary detach ML faces and re-queue recognition (manual faces kept).
- List/statistics/name-search cover own + timeline-visible member-space people; rename/merge/delete of space people: any member. PeopleV1/AssetFacesV2 sync include member-space rows (additive `spaceId`).

## Files changed
- New: `server/src/schema/migrations/1789426700280-SpacePeople.ts`, `server/src/services/person-space.spec.ts` (10 tests).
- Upstream-modified: `schema/tables/{person,person-audit}.table.ts`, `schema/functions.ts`, `database.ts`, `dtos/{person,sync}.dto.ts`, `repositories/{person,access,search,sync}.repository.ts`, `services/{person,asset,search,metadata}.service.ts`, `services/person.service.spec.ts` (3 S1-style fixture updates), `test/{mappers,utils.ts,factories/person.factory.ts}`.
- Fork-owned: `schema/tables/shared-space.table.ts` (+`clusterGroupId`), `schema/migrations/ORDER`.

## FORK.md patch-list lines
`server/src/{schema,services/dtos/repositories}.ts | space-scoped people, recognition, move detach, sync | S9 sketch §11 | S9`

## Deviations / open issues
- Legacy faces stay per-owner (§11 preserved for old data); EXIF-Region import matching stays owner-scoped.
- No per-space sync backfill/complete events (members get space people via normal PeopleV1 stream); clients that left a space keep stale rows until delete-audit prune.
- OpenAPI/SDK/SQL regen deferred (`mise`/Java unavailable, same as S6).
- Search-by-person `personId` filters compose with container scope (no change needed); explore has no people query.
- Medium/e2e tiers NOT RUN (no Docker/sandbox sockets) — needs `scripts/fork-test/run.sh unit medium e2e-api coverage` on host (T0 harness in progress in parallel).

## Self-check
- `pnpm exec tsc --noEmit` clean; `eslint` clean on all touched files.
- 7 suites, 333 tests pass (incl. new `person-space.spec.ts` 10/10).
