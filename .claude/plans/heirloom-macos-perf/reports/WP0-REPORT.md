# WP0-REMAINDER Report (Wave 1)

Branch `perf/heirloom-macos-wp0rest` (worktree `/Users/spatel/workspace/github/projects/immich-wp0rest`),
based on `8d8dd706f` (WP0 logging-API slice merged). Implements WP0-TOOLING.md steps 1, 3, 4, 5.
Step 2 (logging) was already merged — skipped. WP1 owns `PhotosCore/**`; no file there was touched.

## Commits (all with `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`)

- `3d030c1b7` chore(macos): build and install Release by default (step 1)
- `b2578678d` feat(macos): add heirloom-perf profiling harness (step 3)
- `b541b24c5` feat(macos): large synthetic fixture via --fixture-seed-count (step 5)
- Step 4 needed no commit: `project.yml` already lists `Heirloom-macOS-UITests`
  in the `Heirloom-macOS` scheme's test action, and `make test-macos-ui` /
  `make test-core` were added with the step-1 Makefile edit.

## Files changed

- `Makefile` — `CONFIGURATION ?= Release`; `-configuration $(CONFIGURATION)` in
  `build-macos` and `test-macos-ui`; `install-macos` installs the exact
  `$(DERIVED_DATA)/Build/Products/$(CONFIGURATION)/Heirloom-macOS.app` (fails if
  missing), quits a running instance first (osascript, 3 s grace, then pkill);
  new `build-macos-debug`, `test-core`, `test-macos-ui`; help text updated.
- `native-apple/project.yml` — macOS app target Release config:
  `ENABLE_DEBUG_DYLIB: NO`, `SWIFT_OPTIMIZATION_LEVEL: -O`,
  `SWIFT_COMPILATION_MODE: wholemodule`, `ENABLE_PREVIEWS: NO`. Debug untouched.
  (`Heirloom.xcodeproj` is gitignored and regenerated by `make xcodegen`; the
  Release settings were verified present in the generated pbxproj.)
- `scripts/heirloom-perf/profile.sh` (new) — cold-launch, pid wait, `xcrun xctrace
  record --template 'Time Profiler' --instrument Hangs --instrument os_signpost
  --time-limit --attach --output /tmp/heirloom-perf/<name>-<ts>.trace`.
- `scripts/heirloom-perf/summarize.py` (new) — TOC-driven export of
  potential-hangs / time-profile / OSSignpostIntervals (+ os-signpost event
  fallback); resolves id/ref dedup for threads, states, frames, binaries,
  backtraces; streams the time-profile table; prints hangs, top-20 owned
  main-thread frames (entry frames skipped), signpost count/p50/p95/max.
- `scripts/heirloom-perf/README.md` (new) — record/summarize usage, WP7 §3
  scenario A–H, `--fixture-seed-count` documentation.
- `native-apple/Apps/macOS/Sources/FixtureSeed.swift` — `requestedCount` parses
  `--fixture-seed-count=<N>` (clamped to 150000); `changes(count:)` defaults to
  it so `MacAppState.seedForSmoke()` is untouched; `baseChanges()` is the old
  fixture byte-for-byte; `generatedChanges(count:)` emits N deterministic assets
  (photo / video+duration / live still+motion pair / screenshot per the
  `TimelineRow(asset:)` rules) spread over 15 years with cycled aspect ratios
  and one reused real thumbhash. Live pairs add ~5% companion rows.

## Test commands and results (all from the worktree)

- `make build-macos CONFIGURATION=Release` — **SUCCEEDED** (run 4×; final binary
  includes all WP0 changes). Release bundle `Contents/MacOS/` contains only
  `Heirloom-macOS`; no `*.debug.dylib`; `codesign -dv` → `TeamIdentifier=599Z443923`.
  Pre-existing warnings only (SwiftUI `Text(+)` deprecation, ICCameraDevice
  downcast, SyncEngine/Nuke/Media dependency-scan notes); none from WP0 files.
- `swift test --package-path native-apple/PhotosCore` (`make test-core`) —
  **129 tests in 18 suites passed**.
- `python3 scripts/heirloom-perf/summarize.py
  /private/tmp/claude-501/heirloom-traces/baseline.trace` — reproduces the
  baseline doc independently: **13 hangs, 12.93 s total, worst 2.00 s**, hang
  rows match 04-baseline-profile.md §1 exactly; main-thread 16,267 Running
  samples with owned-frame ranking matching §4 (`body.getter` 3880,
  `gridView.getter` 3265, `Asset.==` 1543, …). Signposts: none (expected — R12).
- `profile.sh` acceptance (adapted — `make install-macos` was **not** run per
  WP rules): `bash -n` clean; the script's exact `xctrace record` invocation
  verified against a dummy pid (exit 0, trace written); `summarize.py` on that
  fresh trace prints the empty-table summary without crashing.
- `make test-macos-ui` — builds and runs; **3/3 MacSmokeTests FAIL, pre-existing**:
  `testSidebarAndGridRender` (`sidebar renders` wait times out),
  `testKeyboardSelectionAndMoveTargets`, `testLibrarySwitcherFiltersGrid`.
  Proven pre-existing by re-running `testSidebarAndGridRender` on the
  true-baseline tree (FixtureSeed stashed): identical failure. The app reaches
  foreground; the sidebar never appears within 30 s.
- `--fixture-seed-count=100000` launch (worktree Release binary, timed): **no
  crash**; first window +2.7 s; `refresh userId=user-alice` logged via unified
  log (seed+apply path runs); RSS climbs 24 MB → **528 MB peak at +20 s** (105k
  changes through `apply()`, transient) then settles to ~17 MB; process stayed
  up 120 s until killed. Second baseline recorded: launch→window 2.7 s, seed
  churn peak 528 MB / resident ~17 MB (pre-WP2/WP3 code).

## Deviations from the brief

- `OSSignpostIntervals` used as the primary signpost source (has
  start+duration+name per row) instead of pairing `os-signpost` begin/end
  events; event-pairing kept as fallback.
- Only one genuine thumbhash string exists in-tree (`MediaPipelineTests`);
  all generated assets reuse it (documented in code + README).
- `profile.sh smoke 20` / `make install-macos` deliberately not executed here
  (shared `/Applications`; orchestrator-only). Equivalent coverage above.
- `make test-macos-ui` uses `$(CONFIGURATION)` (default Release); brief shows
  no explicit configuration.

## Open issues (not WP0's to fix)

1. MacSmokeTests fail identically with and without WP0 changes — sidebar never
   renders in this environment. The seeded app reaches foreground and runs
   `refresh()` (proven via `HeirloomLog` debug stream), but no sidebar appears;
   needs WP2 (loader) / WP4 (functional) triage on the host. Do not treat as
   WP0 regression.
2. A directly-executed seeded instance once exited 0 at +20 s (`proc_exit`,
   voluntary; no `terminate`/`exit` call exists in app code) with 5 owned
   windows; a repeat run stayed up 120 s. Smells like system automatic/sudden
   termination of a NotVisible instance in this session — host re-check during WP7.
3. First `make build-macos` in a fresh worktree fails: `openapi.yaml` is
   gitignored and must be generated first (`sh native-apple/scripts/gen-api.sh`).
   Pre-existing worktree-setup gap; consider a Makefile prerequisite.
